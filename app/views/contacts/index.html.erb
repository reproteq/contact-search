<h4>Lista de contactos</h4>
   <div id="resultados" class="alert alert-primary" role="alert">
    <%= @palabrabuscada %> 
   </div>
    
    <script>
        $ (document).ready(function() {
           console.log("jquery");
           
            $("#resultados").click(function() {
                alert ("jquery");
            });
        });
    </script>
 
<p><%= link_to("Añadir contacto", "new_contact_path", data: { turbolinks: false } , :class => "btn btn-success" ) %></p>

<i class="fab fa-facebook-f fa-3x mx-2"></i>
<table class="table table-hover" summary="Contact List">  
  <thead>
    <tr class="header">
      <th>Nombre</th>
      <th>Apellidos</th>
      <th>Calle</th>
      <th>Poblacion</th>
      <th>Provincia</th>
      <th>Pais</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.nombre %></td>
        <td><%= contact.apellidos %></td>
        <td><%= contact.calle %></td>
        <td><%= contact.poblacion %></td>
        <td><%= contact.provincia %></td>
        <td><%= contact.pais %></td>
        
         <td><%= link_to("Ver", contact, data: { turbolinks: false }, :class => "btn btn-primary") %> </td>
         <td><%= link_to("Editar", edit_contact_path(contact), data: { turbolinks: false }, :class => "btn btn-success") %>  </td>
        <td><%= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

